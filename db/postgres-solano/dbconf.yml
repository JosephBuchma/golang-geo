test:
  openStr: user=$TDDIUM_DB_PG_USER dbname=points sslmode=disable

  # TODO: investigate as to why goose requires an 'open' key instead of 'openStr'
  open: user=$TDDIUM_DB_PG_USER dbname=points sslmode=disable

  driver: postgres
  table: points
  latCol: lat
  lngCol: lng
